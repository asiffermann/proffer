<Project>
  <Target Name="XmlDocToMarkup" AfterTargets="AfterBuild" Condition="false">
    <Exec Command="dotnet tool restore" />
    <Exec Command="dotnet xmldoc2md &quot;$(TargetPath)&quot; ../../docs/code/$(DocPath) --github-pages --back-button" />
  </Target>
</Project>