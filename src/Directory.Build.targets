<Project>
  <Target Name="XmlDocToMarkup" AfterTargets="AfterBuild">
    <Exec Command="dotnet tool restore" />
    <Exec Command="dotnet xmldoc2md &quot;$(TargetPath)&quot; ../../docs/_pages/$(DocPath)/api --github-pages" />
  </Target>
</Project>