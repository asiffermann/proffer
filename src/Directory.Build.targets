<Project>
  <Target Name="XmlDocToMarkup" AfterTargets="AfterBuild">
    <Exec Command="dotnet tool restore" />
    <Exec Command="dotnet xmldoc2md &quot;$(TargetPath)&quot; ../../docs/api/$(DocPath)" />
  </Target>
</Project>